export default {
  cdnPrefix: 'https://asset.txqn.huohua.cn/',
  // hiDPI: false,
  bucket: 'huohua-asm',
  // tokenURI: `${token_prefix}:${token_port}/api/upload/token?bucket=huohua-asm`,
  // tokenURI: `${token_prefix}:${token_port}/api/upload/token?bucket=im-assets`,
}
